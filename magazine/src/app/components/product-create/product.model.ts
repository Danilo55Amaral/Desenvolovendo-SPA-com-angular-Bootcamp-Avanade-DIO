// classe de modelo 
// tipo  de recurso do typeScript para definir atributos da interface
export interface Product {
    id?: number
    name: string
    price: number
    quantidade: number
    marca: string
    fabricante: string
}